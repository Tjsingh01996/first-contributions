apiVersion: v1
kind: Service
metadata:
  name: {{ include "kaakaskasjas.fullname" . }}
  labels:
    {{- include "kaakaskasjas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kaakaskasjas.selectorLabels" . | nindent 4 }}
